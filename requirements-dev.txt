# Development and testing dependencies

# Testing
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27

# App server (local dev)
uvicorn[standard]>=0.30

# Web framework and ORM (for tests and local dev)
fastapi>=0.111
starlette>=0.37
sqlalchemy>=2.0

# Databases (local dev)
psycopg2-binary>=2.9
neomodel>=5.1

# Lint/format (optional but recommended)
black>=24.8
ruff>=0.6
